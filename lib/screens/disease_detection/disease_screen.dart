import 'package:flutter/material.dart';
import 'package:image_picker/image_picker.dart';

class DiseaseScreen extends StatefulWidget{const DiseaseScreen({super.key});@override State<DiseaseScreen> createState()=>_DiseaseScreenState();}
class _DiseaseScreenState extends State<DiseaseScreen>{XFile? image;bool analyzing=false;bool result=false;
Future<void>pick()async{await showModalBottomSheet(context:context,builder:(_)=>SafeArea(child:Wrap(children:[ListTile(leading:const Icon(Icons.camera_alt),title:const Text('Take photo'),onTap:()async{Navigator.pop(context);image=await ImagePicker().pickImage(source:ImageSource.camera);setState((){});}),ListTile(leading:const Icon(Icons.photo_library),title:const Text('Choose from gallery'),onTap:()async{Navigator.pop(context);image=await ImagePicker().pickImage(source:ImageSource.gallery);setState((){});})])));}
void analyze(){setState(()=>analyzing=true);Future.delayed(const Duration(seconds:2),(){if(mounted)setState((){analyzing=false;result=true;});});}
@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('AI Disease Scan',style:TextStyle(fontWeight:FontWeight.w900))),body:ListView(padding:const EdgeInsets.all(20),children:[Container(height:240,decoration:BoxDecoration(color:Colors.green.shade50,borderRadius:BorderRadius.circular(26)),child:image==null?Column(mainAxisAlignment:MainAxisAlignment.center,children:[const Icon(Icons.document_scanner_rounded,size:70,color:Colors.green),const SizedBox(height:12),const Text('Scan a leaf to identify disease',style:TextStyle(fontWeight:FontWeight.w800)),const SizedBox(height:6),Text('AI-powered crop health analysis',style:Theme.of(context).textTheme.bodySmall)]):FutureBuilder<List<int>>(future:image!.readAsBytes(),builder:(context,snapshot)=>snapshot.hasData?ClipRRect(borderRadius:BorderRadius.circular(26),child:Image.memory(snapshot.data as dynamic,fit:BoxFit.cover)):const Center(child:CircularProgressIndicator()))),const SizedBox(height:18),Row(children:[Expanded(child:OutlinedButton.icon(onPressed:pick,icon:const Icon(Icons.upload_file),label:const Text('Upload image'))),const SizedBox(width:10),Expanded(child:FilledButton.icon(onPressed:image==null||analyzing?null:analyze,icon:analyzing?const SizedBox(width:18,height:18,child:CircularProgressIndicator(strokeWidth:2)):const Icon(Icons.auto_awesome),label:Text(analyzing?'Analyzing...':'Analyze')))]),if(result) ...[const SizedBox(height:24),Container(padding:const EdgeInsets.all(20),decoration:BoxDecoration(color:Theme.of(context).colorScheme.surface,borderRadius:BorderRadius.circular(24)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Row(children:[const CircleAvatar(backgroundColor:Color(0xFFFFF3E0),child:Icon(Icons.warning_amber_rounded,color:Colors.orange)),const SizedBox(width:12),const Expanded(child:Text('Early Blight detected',style:TextStyle(fontSize:20,fontWeight:FontWeight.w900))),const Text('94%',style:TextStyle(color:Colors.green,fontWeight:FontWeight.w900))]),const SizedBox(height:18),const Text('Symptoms',style:TextStyle(fontWeight:FontWeight.w800)),const Text('Brown circular spots with yellowing around affected leaf areas.'),const SizedBox(height:14),const Text('Recommended treatment',style:TextStyle(fontWeight:FontWeight.w800)),const Text('Remove affected leaves, improve airflow, and apply a suitable approved fungicide according to its label.'),const SizedBox(height:14),const Text('Prevention',style:TextStyle(fontWeight:FontWeight.w800)),const Text('Avoid overhead watering and inspect lower leaves every 2–3 days.')]))]));
}